extends view_base

block region
    h1=t('step password title')
    p=t('step password description')
    input(type="password", name="password")
    footer
        .controls
            button=t('step password submit')
        .progression
