extends view_base

block region
    h1=t('step password title')
    input(type="password", name="password")
    footer
        .controls
            button=t('step password submit')
        .progression
